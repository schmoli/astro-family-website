---
import Layout from "~/layouts/Layout.astro";
import { SITE } from "~/config.mjs";
import Starfield from "~/components/starfield.astro";
import Icon from "astro-icon";

const title = `Error 404 — ${SITE.name}`;
---

<Layout meta={{ title }}>
  <Starfield />
  <div class="flex justify-center items-center h-screen relative flex-col">
    <Icon name="openmoji:desert-island" class="h-72" />
    <p class="text-4xl font-bold tracking-tight gradient-text sm:text-5xl">
      404: Not Found
    </p>
    <a
      href="/"
      class="inline-flex items-center rounded-md border border-transparent bg-blue-600 px-4 py-2 text-sm font-medium text-white shadow-sm hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2"
    >
      Go back home
    </a>
  </div>
</Layout>
